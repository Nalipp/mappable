# Record index for mappable-records.json
#
# highest_id_ever_issued only increases. When finalizing a new record, use
# highest_id_ever_issued + 1 as its ID, then update this counter. Never reuse
# an ID, even after a record is deleted from mappable-records.json.
highest_id_ever_issued: 0

# Completed datasets and their constraint profiles (one line per finalized
# dataset): <dataset file> | <constraint profile> | <finalized date>
